
void printutil(){
    for(int i=0; i<list.size(); i++){
        
    }
}
void print(vector<vector<string>> list){
    vector<string> v;
    for(int i=0; i<list[0].size(); i++)
        printutil(v, 0, list, i);
}
